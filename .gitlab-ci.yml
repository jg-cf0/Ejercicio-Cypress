stages:
  - test

cypress_test:
  stage: test
  image: cypress/included:13.0.0
  script:
    - cypress run --spec "cypress/e2e/todo.cy.js"
  artifacts:
    paths:
      - cypress/screenshots/
      - cypress/videos/
    when: always